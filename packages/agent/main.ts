export function prompt(params: {
  prompt: string;
  roles: string[];
  assertionToken: string;
}) {
  console.log("AGENT: Received prompt", params);

  // TODO: Implement agent.
  return "--agent response goes here--";
}
